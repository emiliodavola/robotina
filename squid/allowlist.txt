# ============================================================================
# Allowlist de egreso. Un dominio por linea; el punto inicial incluye el
# dominio y todos sus subdominios.
#
# Todo lo que no este aca se deniega. Al agregar un destino preguntate si ese
# servicio necesita ver el trafico de los agentes; si la respuesta es "no se",
# no lo agregues.
# ============================================================================

# --- Telegram (Hermes gateway / Bot API, incluye descarga de archivos) -------
# Verificado: la imagen trae python-telegram-bot 22.8 (Bot API sobre HTTPS),
# que pasa proxy=None a httpx y por lo tanto honra HTTPS_PROXY.
.telegram.org

# --- Proveedor de modelos (OpenCode Go / Zen) --------------------------------
# Verificado en la imagen: el relay es opencode.ai
# (agent/anthropic_endpoints.py, agent/model_metadata.py:
# "opencode.ai": "opencode-go"). Cubre api.opencode.ai si lo usa.
.opencode.ai

# --- Repositorios y paquetes (git, npm, pip) --------------------------------
# El punto inicial ya cubre el dominio y todos sus subdominios: no hace falta
# listar api.github.com, codeload.github.com ni objects.githubusercontent.com.
.github.com
.githubusercontent.com
.registry.npmjs.org
.pypi.org
.files.pythonhosted.org

# --- MCP remotos de opencode -------------------------------------------------
# context7 (documentacion) y gh_grep (busqueda de codigo en GitHub) son los dos
# MCP que el agente consulta por HTTP. Salen a un tercero: mandan la CONSULTA,
# no el repo. Si preferis cerrarlos, comentá estas lineas y el agente va a
# reportar 403 al usarlos.
mcp.context7.com
mcp.grep.app

# --- Pedidos por Hermes y denegados a proposito ------------------------------
# Observado con el proxy arriba: Hermes intenta estos tres al arrancar. Los 403
# no le impiden funcionar (sigue con sus 58 skills ya incluidas y sin errores),
# asi que quedan cerrados hasta que decidas lo contrario. Descomentar solo si
# los necesitas:
#
# openrouter.ai                 # catalogo de modelos, si tu config apunta ahi
# portal.nousresearch.com       # portal de Nous (auth / creditos)
# hermes-agent.nousresearch.com # canal de update / docs de Hermes
